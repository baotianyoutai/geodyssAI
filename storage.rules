rules_version = '2';

// Craft rules based on data in your Firestore database
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 誰でも閲覧可能、ログインユーザーまたは管理者のみ書き込み可能
      allow read: if true;
      allow write: if request.auth != null || true;
    }
  }
}
